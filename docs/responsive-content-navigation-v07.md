# v07 — Summary factual copy and right header actions

Correcciones:

- El texto del resumen ya no describe lo que la sección “debe hacer”; ahora resume el trabajo real del perfil.
- `Resumen de la sección` pasa a `Síntesis profesional` o títulos específicos desde `section-summaries.ts`.
- El header de escritorio organiza: marca izquierda, navegación centro, idioma/modo derecha.
- En pantallas pequeñas se mantiene `ES`, no `Español`, para evitar recortes.
- Se refuerza el control de overflow horizontal en tarjetas y secciones.
